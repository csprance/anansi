import { PlacesState } from './types';

const defaultState: PlacesState = {
  0: {
    world: 0,
    id: 0,
    name: 'Casa Del Chris',
    description: 'This is the coolest place where Chris usually hangs out at',
  },
};

export default defaultState;
